HANDOFF — AINode Product Threadmaster Session 3

Repo: getainode/ainode
Date: 2026-04-13
Author: Claude Opus 4.6 (Threadmaster, session 3)

## Session Summary

Massive session: completed v0.1.0 build, ran gap analysis against EXO, security hardened, deployed to real ASUS GX10 hardware, got vLLM inference running via Docker container, began NVIDIA-themed UI redesign.

## What Was Done

### Hardware Deployment (ASUS GX10)
- SSH access established, Tailscale installed (100.72.9.84)
- GPU detected: NVIDIA GB10, 122 GB unified memory, CUDA 13.0, SM 12.1
- AINode installed, 290/291 tests pass on device
- Static IPs set: .112 (mgmt), .220 (fabric1), .66 (fabric2)
- Fabric Connect 7 links UP at 100 Gbps each, jumbo frames (MTU 9000) working
- iperf3: 13 Gbps with 0 retransmits over fabric
- Routing fixed: fabric preferred (metric 50) over management (metric 200)
- Connectivity to both Sparks (.188, .219) and ROSA (.100) verified

### vLLM on GB10
- pip vllm wheel is built for CUDA 12, GB10 has CUDA 13 only — incompatible
- Native source build attempted but requires ~15+ hours (952 CUDA kernel targets on ARM64)
- **Solution: Docker container** — `scitrera/dgx-spark-vllm:0.17.0-t5` running on port 8000
- Qwen 2.5 3B serving inference successfully
- AINode web server running on port 3000, proxying to vLLM container

### UI Redesign (In Progress)
- 4-agent rewrite: style.css, index.html, topology.js, app.js — all completed (PR #30)
- NVIDIA color palette (#76B900 green on #0a0a0a black), command-center layout
- Multiple polish passes deployed live to GX10
- Jason's feedback captured — still needs work (see "What's Next")

## Current State

### Running on GX10 (192.168.0.112)
- vLLM container: `sudo docker run -d --name vllm-server --gpus all -p 8000:8000 -v ~/.cache/huggingface:/root/.cache/huggingface scitrera/dgx-spark-vllm:0.17.0-t5 python3 -m vllm.entrypoints.openai.api_server --model Qwen/Qwen2.5-3B-Instruct --host 0.0.0.0 --port 8000 --dtype bfloat16`
- AINode web: `nohup python3 -c "from ainode.api.server import run_server; from ainode.core.config import NodeConfig; run_server(NodeConfig.load())" > /tmp/ainode-web.log 2>&1 &`
- Deploy script at /tmp/deploy.sh on GX10

### Branch State
- `dev` branch has the latest UI redesign + polish commits
- `main` is behind dev (hasn't been promoted since the redesign)

## What's Next (Priority Order)

### 1. Model Catalog — CRITICAL
Jason's feedback: "We don't have the full load of all models available through vLLM. Shouldn't we have a much bigger catalog?"
- Current catalog: only 6 hardcoded models in ainode/models/registry.py
- Need: dynamic catalog from HuggingFace, searchable, filterable
- The model list rendering is ugly — needs proper row/card design with consistent spacing
- Downloaded filter shows nothing (because models are in Docker container, not on host)
- Recommended filter shows nothing (GPU detection issue when no engine reference)

### 2. Model List UI — CRITICAL
Jason's feedback: "These should be rows. They don't look good."
- Model items need card-style rows: model name left, size + status right, download button aligned
- Consistent padding, proper typography hierarchy
- Expandable detail on click

### 3. UI Polish Remaining
From Jason's live feedback on the deployed UI:
- ✅ Fixed: Bottom dropdown styled, corners softened, NEW CHAT rounded
- ✅ Fixed: Conversation list items styled, delete button hidden until hover
- Still needed: model list proper row styling, larger catalog, recommended working

### 4. Docker Integration
- AINode currently launches vLLM as a subprocess (broken on CUDA 13)
- Need to update engine to optionally manage a Docker container instead
- Config flag: `engine_mode: "subprocess" | "docker"`
- Docker image: `scitrera/dgx-spark-vllm:0.17.0-t5`

### 5. Install Script
- Updated install.sh (committed to main) detects GB10 and attempts source build
- Need to add Docker container fallback path for CUDA 13 systems
- The source build is not viable for end users (~15 hours)

## Network Map

| Node | Management | Fabric 1 | Fabric 2 | Tailscale |
|------|-----------|----------|----------|-----------|
| GX10 | .112 | .220 (100G) | .66 (100G) | 100.72.9.84 |
| Spark 1 | .110 | .188 | .63 | 100.122.26.9 |
| Spark 2 | .111 | .219 | .65 | 100.81.184.19 |
| ROSA | .100/.101 | — | — | — |
| R750 | .8 or .18 | — | — | 100.110.83.82 |

## Credentials
- GX10 SSH: sem@192.168.0.112 or sem@100.72.9.84, pw: Dragon@123!@#
- Same credentials work for Spark 1 (100.122.26.9) and Spark 2 (100.81.184.19)

## Risks
- Docker container approach means vLLM version is pinned to what scitrera builds
- ROSA model storage (NVMe-TCP) not connected to GX10 yet — need to set up initiator
- The 86 models on ROSA are MLX format (from EXO) — vLLM needs SafeTensors format
- UI redesign still in progress — Jason not satisfied with model list quality
